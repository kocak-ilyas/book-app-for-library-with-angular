export interface Book {
  // imageUrl?: string;
  // available?: boolean;
  // page?: number;
  // date?: any;
  // explanation?: string;

  name: string;

  address?: string;
  email?: string;
  id?: string;
  phone: string;
  title: string;
}
